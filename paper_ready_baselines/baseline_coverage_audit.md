# Baseline Coverage Audit

Generated by `baseline_suite/audit_baseline_coverage.py`.

## Input Sanity

- Result JSON files inspected under `result/final-run/`: 43.
- Methods found in result JSON text: classifier_real_only, hybrid_diversity_buffer, hybrid_random_buffer, malcl_like, plus_kd, real_buffer_only, wgan_projection_generated_only.
- Missing required audit inputs: none.
- JSON parse errors: 0.

## Coverage Table

| baseline_name | category | status | matched_existing_method | needs_new_run | needs_external_code | recommended_action |
| --- | --- | --- | --- | --- | --- | --- |
| Fine-tuning / None | A mandatory | already covered | classifier_real_only | NO | NO | Use as the None/fine-tuning baseline, and label it as no replay rather than an external paper baseline. |
| Joint / offline upper bound | A mandatory | missing |  | YES | NO | Implement a same-MLP offline upper bound trained on all classes or cumulative data under the exact split; keep it out of memory-budget fairness claims. |
| Experience Replay / ER | A mandatory | partially covered | real_buffer_only | YES | NO | Run canonical ER with random/reservoir real replay and no generated replay, KD, prototype alignment, or diversity selection. |
| Pure generated replay | A mandatory | already covered | wgan_projection_generated_only | NO | NO | Use as the internal generated-only replay baseline and drift comparator. |
| MalCL faithful | A mandatory | needs faithful reproduction | malcl_like | YES | YES, unless reimplemented locally from the paper | Treat malcl_like as an internal baseline; reproduce faithful MalCL if reviewers require the closest malware-specific generative replay competitor. |
| DER++ | A mandatory | missing |  | YES | NO for a local custom runner; optional to consult Avalanche/Mammoth | Implement and run DER++ with replay buffer, stored logits, replay CE, and logit MSE. |
| MADAR | A mandatory | needs external code |  | YES | YES | Integrate MADAR source only after explicit approval; adapt to EMBER/AZ class stream, memory budgets, and output schema. |
| iCaRL | B strong recommended | missing |  | OPTIONAL | NO if adapted locally | Scaffold optional runner; run if GPU time remains after DER++, MADAR, ER, Joint, and MalCL decision. |
| LwF | B strong recommended | missing |  | OPTIONAL | NO if adapted locally | Scaffold optional runner; consider as replay-free distillation comparator. |
| EWC | B strong recommended | missing |  | OPTIONAL | NO if adapted locally | Scaffold optional runner; lower priority than replay baselines. |
| FreeMOCA | B strong recommended | literature-only |  | OPTIONAL | YES | Discuss as recent malware-specific replay-free work unless GPU/integration time permits exact-protocol reproduction. |
| GDumb | C optional | literature-only |  | OPTIONAL | NO if adapted locally | Keep as optional memory-only sanity baseline; not central to replay-drift hypothesis. |
| SI | C optional | related-work-only |  | NO | NO | Do not prioritize unless reviewers explicitly ask for additional regularization baselines. |
| Avalanche / Mammoth | framework reference | related-work-only |  | NO | NO | Use only as implementation reference for DER++, ER, EWC, LwF, and iCaRL. |

## Key Decisions

- `classifier_real_only` is enough for the None/fine-tuning baseline because it has no replay, no real buffer, no diversity buffer, no KD, and no prototype alignment in the trusted ablation evidence.
- `real_buffer_only` is not enough for canonical ER because the trusted flags show diversity-buffer, KD, and prototype-alignment behavior; keep it as an ablation and run clean ER.
- `wgan_projection_generated_only` is enough for pure generated replay in this architecture, but it is not a faithful MalCL reproduction.
- `malcl_like` is useful internal evidence, but faithful MalCL should be reproduced if the paper needs the closest malware-specific generative replay competitor.
- DER++ and MADAR are currently missing as exact-protocol external baselines.

## Full CSV

The full decision matrix is in `paper_ready_baselines/baseline_coverage_audit.csv`.
