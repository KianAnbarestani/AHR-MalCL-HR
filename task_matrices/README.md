# Released task matrices

Each JSON is one method–dataset–seed record. Rows 1–11 are post-training stages; columns 1–11 are evaluation tasks. The observed lower triangle contains sample-level task accuracies as fractions. Future-task cells are JSON `null`. No raw features, sample identifiers, filenames, executable paths, or private URLs are included.
