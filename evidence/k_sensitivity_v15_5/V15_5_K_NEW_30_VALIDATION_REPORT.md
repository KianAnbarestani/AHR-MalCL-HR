# New V15.5 K-sensitivity validation

The compact archive hash was independently checked against the supplied SHA256. All 30 required run directories contain the scientific artifacts and `VALIDATED_COMPLETE`; all registry identities, 11-task matrices, finite lower triangles, checksums, Tesla-T4/Python/PyTorch metadata, class orders, Full-Hybrid component flags, K values, generated quota, GAN schedule, sampler, epoch, and disabled auxiliary-feature checks passed. New runs validated: 30/30; failures: 0.

The archive's per-run `status.json` remains `COMPLETED_UNVALIDATED`; `VALIDATED_COMPLETE` is the final transferred validation marker.
